[package]
name = "pinkdown"
version = "1.2.0"
edition = "2021"
authors = ["PinkDown"]
description = "A polished, native Markdown reader and editor in Rust."
license = "MIT"

[dependencies]
chardetng = "0.1"
encoding_rs = "0.8"
eframe = { version = "0.32", default-features = false, features = ["default_fonts", "glow", "x11", "wayland"] }
egui_commonmark = { path = "vendor/egui_commonmark", default-features = false, features = ["pulldown_cmark"] }
image = { version = "0.25.9", default-features = false, features = ["png"] }
pulldown-cmark = { version = "0.13", default-features = false }
rfd = "0.15"
semver = "1"
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
ureq = { version = "2.12", features = ["json"] }

[build-dependencies]
winres = "0.1"

[target.'cfg(windows)'.dependencies]
raw-window-handle = "0.6"
windows-sys = { version = "0.59", features = [
    "Win32_Foundation",
    "Win32_Graphics_Dwm",
    "Win32_UI_WindowsAndMessaging",
] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
